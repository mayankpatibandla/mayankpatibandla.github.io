{
  "author": "Mayank Patibandla",
  "description": "Mayank Patibandla's Website",
  "license": "ISC",
  "main": "index.ts",
  "name": "mayank-patibandla-website",
  "scripts": {
    "build": "npm run lint && npm run format && npm run compile",
    "compile": "tsc -p ./tsconfig.json",
    "format": "prettier --write --cache --config .prettierrc **/*",
    "lint": "eslint --fix --config .eslintrc scripts/**/*.{js,ts}"
  },
  "version": "1.0.0"
}
